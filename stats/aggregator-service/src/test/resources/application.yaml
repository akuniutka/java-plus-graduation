spring:
  main:
    banner-mode: off
  cloud:
    config:
      enabled: false
kafka:
  topics:
    actions: stats.user-actions.v1
    similarity: stats.events-similarity.v1
eureka:
  client:
    enabled: false
